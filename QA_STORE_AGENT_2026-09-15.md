# 门店 AI 助手线上回归报告（2026-09-15）

环境：`inventory-count-closure-demo.gdjustwuxia.workers.dev`。Demo 工作流状态由 R2 保存；D1 保留为历史关系数据。以下接口均未调用或写入飞书。

## 结论

核心的“先形成草稿、店员确认后才写业务流水”已通过。多轮聊天在同一个 `session_id` 下保存在 R2，并可恢复草稿。调拨、收货、补货和报损均有确认门槛。

## 已执行用例

| 用例 | 实际结果 | 判定 |
| --- | --- | --- |
| `黑糖珍珠烂了` → `2kg` → `过期` | 依次形成物料、数量、原因；草稿为“黑糖珍珠 / 2kg / 过期”，由 UI 要求上传凭证后确认。 | 通过 |
| 报损凭证与确认 | 上传模拟 PNG 后没有库存流水；确认生成 `SC-20260915-E3C8`，撤销后活动流水恢复为 0。 | 通过 |
| `调 2kg 黑糖珍珠去 STORE002` | 返回 `open_transfer` 草稿；提交前调拨单和库存流水数都没有变化。 | 通过 |
| 一对多调拨完整链路 | `TR-20260915-58FA40`：STORE002 收 0.2kg、STORE003 收 0.3kg；状态依次为待收货、部分收货、已完成。 | 通过 |
| `我要补货` → `牛奶` → `20L` → `库存不足，晚高峰前需要` | 连续补齐物料、数量、原因和紧急程度；未确认请求被拒绝，确认后创建 `RST-20260915-001`，不产生库存流水。 | 通过 |
| `查牛奶库存` | 返回系统理论库存 `130 L`，仅读取 R2 演示台账快照。 | 通过 |
| `我要报损` → `牛奶` → `2L` → `过期` | R2 会话保存 8 条消息，最终草稿为 `scrap / 牛奶 / 2L / 过期`。 | 通过 |
| 可调余量不足 | 尝试调拨 999kg 被拦截，未新增调拨单。 | 通过 |
| 重复收货 | 对已收货明细再次确认被拦截，未重复入库。 | 通过 |

## 本次留下的可追溯 Demo 数据

- 测试报损照片凭证：`PD-EB502037`；对应库存流水已经撤销，不影响理论库存。
- 补货申请：`RST-20260915-001`，状态为 `pending_hq_review`。
- 一对多调拨：`TR-20260915-58FA40`，已完成；STORE001 调出黑糖珍珠 0.5kg，STORE002/003 已按实际数量收货。

## 仍需补测 / 上线门禁

1. 盘点照片视觉识别：需要配置 `DOUBAO_VISION_MODEL` 为火山方舟视觉模型接入点。当前文本 GLM 可作为聊天模型，但不应作为密集盘点单 OCR 的验收模型。
2. 视觉模型测试应覆盖：清晰照片全识别、模糊照片仅提示人工确认、错误单位、重复物料、非盘点照片和超大文件。
3. 浏览器端应做一次人工验收：取消当前草稿、切换底部 Tab 后草稿行为、网络中断重试、移动端相册与相机权限。
4. 后续生产化需补：角色鉴权、幂等键、真实图片对象存储、操作人身份和审计导出。
