# QA 回归报告（2026-09-16）

## 结论

- 本地自动回归：13/13 通过。
- D1：13 个迁移均可在本地完整执行；迁移后健康检查显示 21 张表、legacy_tables_present=true。
- R2：本地主流程可独立运行，健康检查可读写 current state。
- 线上远程 D1 状态：尚未核实。当前非交互环境没有 CLOUDFLARE_API_TOKEN，不能安全地查询或应用远程迁移。

## 自动测试覆盖

1. R2/D1 存储健康。
2. 6 家门店、18 个商品、20 个物料。
3. 12 项每日盘点、8 项按需盘点。
4. 盘点策略可修改并恢复。
5. 工单来源的两物料不定期盘点可下发。
6. 盘点计划、库存单据、门店端页面可访问。
7. 中文调拨识别。
8. English 调拨与库存查询。
9. Bahasa Indonesia 调拨与库存查询。
10. 两句式调拨上下文继承。
11. 印尼语两句式报损上下文继承。
12. kg→g 多单位换算：2 kg 黑糖珍珠转换为 2000 g。
13. 模糊语句不会直接写库存。

执行命令：

    BASE_URL=http://127.0.0.1:8787 MUTATION_TESTS=1 node tests/regression.mjs

## HTML 语音能力

### 页面检查

- 麦克风按钮存在。
- 语言选择包含 zh-CN、en-US、id-ID。
- 每次开始录音前按当前选项设置 recognition.lang。
- 不支持时保留文字输入，并向用户说明当前浏览器不支持。

### 浏览器实测

Codex 内置浏览器检测结果：

- SpeechRecognition：undefined
- webkitSpeechRecognition：undefined
- 结论：该浏览器完全不支持 Web Speech API，并非印尼语单独失败，因此无法在此环境做真人录音转写。

正式验收应使用最新版 Chrome/Edge，通过 HTTPS 分别说：

- English: “Transfer 2 kg of brown sugar pearls to STORE002.”
- Bahasa Indonesia: “Pindahkan 2 kg mutiara gula merah ke STORE002.”
- 中文：“调 2kg 黑糖珍珠去 STORE002。”

若目标设备对 id-ID 的 Web Speech 支持不稳定，第二阶段应改为 MediaRecorder 上传音频至服务端 ASR；客户端只负责录音和语言标记，服务端转写后仍进入同一个草稿确认流程。不能让 ASR 结果直接写库存。

## 多轮与边界

- 已修复：服务端保存的 session draft 在确定性兜底中未被继承。
- 已修复：STORE002 中的数字不能误识别为数量 2。
- 已修复：未初始化台账时从物料主档识别物料，而不是返回 undefined。
- 已修复：口述采购/常用单位与基础库存单位不一致时显式换算。
- 报损和收货照片改为可选；盘点照片仍必填。

## 尚需线上验证

1. Cloudflare 重新授权后部署。
2. 只读核对远程 D1 迁移列表；先备份再决定是否 apply。
3. 在真实 Chrome/Edge 授权麦克风后完成三语言真人录音。
4. 用线上现有 R2 状态做只读回归；变更型用例仍只在本地运行。
