# 库存台账 Demo 状态与待办

## 当前最高优先级：研判双轨闭环（2026-09-17）

设计基线见：

- `docs/库存研判双轨闭环实施主线_v1.md`
- `docs/决策树规则模块设计_v1.md`
- `docs/参考资产审阅与启用清单_v1.md`
- `docs/库存研判闭环设计_v1.md`

按以下顺序实施，不同时粗做四棵树：

- [ ] Phase 0：定义事实包、决策树、研判案例、动作注册表和 AI Review Schema；修订豆包规则包的量纲与安全边界。
  - [x] 第一步：增加 V1 行为保护测试，锁定四类规则优先级、黑糖珍珠 D2 证据字段和现有工单文案映射。
  - [x] 第二步：定义事实包、决策树、研判案例、动作注册表和 AI Review 数据契约；测试明确区分确认值 0、未接入和未知。
  - [x] 第三步：抽出只读事实聚合器，统一生成今日、近 3 日和近 7 日证据窗口。
  - [x] 第四步：建立受控 D2 决策执行器，保留触发、库存重建、贡献排序、入库覆盖、实盘和动作轨迹。
  - [x] 第五步：增加 D2 V2 影子接口；不写 R2、不建工单、不替换 V1。
  - [x] 第六步：在现有库存研判页增加 V1/V2 对照预览；不增加一级导航，支持总览和单个 D2 的“查看 V2 推演”。
  - [x] 第七步：新增规则单元测试全部通过；完整端到端回归 32/32 通过，影子接口明确 `writable=false`、`creates_work_orders=false`。
  - [x] 第八步：已部署线上并核对真实黑糖珍珠 D2 案例；V2 将调拨出库 13,000g 识别为第一贡献项，首要位置为“调拨出库与目标门店签收”，接口保持只读且不建工单。
  - [x] 第八步补充：V2 影子执行扩展到 D1 / D2 / S1 / T2 四类；页面中文化规则枚举，并区分未找到记录、历史覆盖不完整、有流水未关联单据及等待签收。
  - [x] 第八步验证：线上四类各命中 1 项；D1/D2/S1 实盘已确认，T2 待盘点；修正“有收货流水但无正式收货单”被误写成零流水的问题。影子接口仍为只读且不建工单。
  - [ ] 第九步：由用户人工查看 V1/V2 页面表现；确认后再让 V2 研判快照进入正式工单详情。
- [ ] Phase 1：以黑糖珍珠为固定案例，完整跑通 D2 的事实聚合、决策轨迹、工单继承、动作链接、重算和人工闭环。
  - [x] 事实聚合、库存重建、贡献排序、决策轨迹及只读动作建议已完成。
  - [x] 建立工单时冻结 V2 研判快照，保存规则版本、证据引用、缺失证据、首要位置和建议动作；工单详情支持手动重新研判并保存多次运行记录。
  - [ ] 动作链接携带门店、物料、日期和来源工单，预填调拨核对、临时盘点、订货及收货草稿。
  - [ ] 业务单据或实盘确认后自动重跑同一案例；当前已支持工单内手动重跑并保存前后多次结果，不覆盖历史。
  - [ ] 人工填写最终原因、解决方式、采纳情况和处理人后闭环；恢复失败时重新打开。
- [ ] Phase 1 验收：覆盖大额调拨、重复调拨、到货未入库、实盘有货、实盘无货、单位错误、数据未接入、查询失败和修正后重算。
- [ ] Phase 2：D1 / S1 / T2 已完成影子主路径；继续补齐反例、缺数据、单位错误、重复流水、截止时间不一致和历史覆盖不足测试。T2 必须持续验证销售 BOM 到物料基础单位的转换。
- [ ] Phase 3：增加手动 AI 复核，精确加载 Knowhow，与规则结果并排交叉验证；禁止 AI 直接写库存或自动闭环。
- [ ] Phase 4：将证据请求、工单草稿、单据关联、重算和人工确认编排为受控 Agent 状态机。

## 今日执行（2026-09-16 至 2026-09-17）

- [x] 建立本地 Git 版本基线 dec1c5f 与标签 baseline-2026-09-16。
- [x] 创建 GitHub 仓库并推送 `main`、完整提交历史与全部回滚标签；2026-09-17 用户确认仓库保持 Public，最终冻结版本已按授权公开推送。
- [x] 盘点物料增加“每日盘点 / 按需盘点”策略与总部自由选择物料下发接口。
- [x] 门店语音增加中文、English、Bahasa Indonesia 选择；补齐麦克风未授权、无语音和服务不可用的三语言提示；报损和收货照片改为可选。
- [x] 提炼库存异常 Knowhow；MVP 只启用 D2 / D1 / S1 / T2 硬规则。
- [x] 完整 Knowhow 已版本化保存并部署为在线知识库页面；MVP 提炼与原始 Markdown 均可查看。
- [x] 13 个 D1 迁移已在本地完整通过；远程 D1 备份后完成 0012/0013，R2 兜底保持健康。
- [x] 完成三语言、多轮上下文、异常边界、HTML 语音绑定、盘点确认、收货/报损撤销、补货确认、工单闭环、通知兜底、Knowhow R2 保存、主数据质量门禁及列表过载检查本地回归；本轮增加 D2 V2 影子对照后端到端回归扩展为 32/32，通过 3 组新增规则单元测试。中文与英文真人录音已验收，印尼语合成音频端到端 3/3 通过。
- [x] 检查全部页面的数据增长风险；为流水、单据、盘点计划、研判、模拟器和调拨增加分页，为首页/物料页增加摘要上限，并增加页面专用轻量接口。
- [x] 完成主数据/BOM/单位换算/单据状态检查与演示顺序手册；Brown Sugar Boba Milk Tea 6 个真实 SKU 的 54 条飞书 BOM 已纳入白名单，其他商品明确标记为 MVP Mock。
- [x] 使用已配置的飞书应用直接读取 BOM 表：线上验证扫描 82 条记录，6 个 Brown Sugar SKU 各 9 条有效配方；增加完整性门禁和快照回退。
- [x] 输出总部飞书账号同步、门店 OAuth / 邮箱登录设计。
- [x] 门店助手完成手机底部固定输入、点击录音沉浸层、权限弹窗防穿透、自动语言识别和转写完成后即时收层。
- [x] 盘点计划与调拨管理拆分为页内 Tab；独立库存流水页承接收货、报损、调拨、盘点和销售筛选。
- [x] 日常运营新增订货管理与收货管理：独立列表/新建 Tab 和单据详情页，支持多物料、订货五状态、关联/独立收货、分批收货和防重复确认；草稿及订货不写库存，仅人工确认收货后生成流水，并预留工单来源与紧急程度。
- [x] 门店助手同步订货/收货单据流程：补货或订货只建立订货草稿，收货只建立收货草稿，可关联待收货订货单；两类草稿均跳转单据详情人工确认，不再由助手直接写入库存。线上 QA 草稿建立/取消前后库存流水数保持 11 不变。
- [x] 跟进工单补齐详情抽屉、时间线、可空操作人、进展记录和关联单据；门店助手支持一次查询多个物料。

## 本轮研判强化（2026-09-15）

- [x] 物料异常增加排除证据：期初来源、上周期负库存期末、收货记录是否存在、BOM 消耗与实盘差异。
- [x] 同一门店持续问题沿用原日结研判单；盘点后仍有差异时追加观察，不新建重复工单。
- [x] 增加“盘点后仍有差异 → 原工单下发定向物料盘点”的 R2 演示接口与页面动作。
- [x] 研判详情可直接下钻门店物料完整流水；无收货记录时明确显示“证据不足”，不直接判定未收货。
- [x] 增加餐饮行业 Know-how 规则层：包装耗材、易耗液体、现制半成品、自然损耗物料、称重原料和一般物料采用不同盘点容差与必查证据。
- [x] 行业判断输出“有证据 / 已排除 / 疑似 / 证据不足 / 待盘点”，并按证据优先级给出下一步，不把行业模板当成真实门店结论。
- [x] 总部可人工强制结案，但必须选择 / 自定义归因，填写判断依据、实际解决方法和处理人；结案内容独立沉淀为 R2 研判知识。
- [x] 新研判单按同物料、同规则、同行业类型和同门店匹配历史人工结案知识，展示相似案例及曾采用的解决方法。
- [ ] 当前每日盘点范围为 12 项（20 项主档减去用户指定的 8 项非必要盘点物料）。购量单位与换算已定版；仍需品牌确认半成品奶茶、黑糖成品、黑糖冻的标准出成率、合理损耗率与保质期。
- [ ] 下一次真实飞书同步后验证：全盘提交、单物料差异、定向盘点提交、下一次回算正常后结案。

## 后续待办（2026-09-16 更新）

### P0：真实环境验证与数据补齐

1. 已上线 `MediaRecorder + Workers AI Whisper` 真人语音验收页及门店助手业务语音入口；中文、English 真人 6/6 通过，标准印尼语合成音频链路 3/3 通过。仍建议由印尼语使用者补充真机口音验收，但不阻塞当前 MVP 语音链路使用。
2. Brown Sugar Boba Milk Tea 6 个真实 SKU 已完成 BOM 白名单；继续确认其二级半成品出成率、合理损耗率和保质期。其余 12 个商品当前是 MVP Mock，不列为品牌 BOM 缺口；未来转正式时再取得配方与生效日期。
3. GitHub 仓库 `joearkon/inventory-count-closure-demo` 按用户确认保持 Public；远端 `main` 和最终冻结标签 `release-2026-09-17-mvp.4` 已推送并核对一致。

### P1：飞书真实事件表接入（暂缓，不阻塞 MVP）

当前尚未建立可用的飞书“收货、报损、调拨事件”正式表，因此本阶段不实施真实事件同步，也不把缺表视为系统故障。

当前只需保留以下准备：

1. 系统内收货、报损、调拨单及其状态继续由 R2 Demo 流程演示，来源明确标记为系统事件，不伪装成飞书真实单据。
2. 保留只读接入契约：门店、营业日、物料、数量、单位、外部单号、状态、更新时间；正式表建立后再做字段映射。
3. 正式接入时必须验证外部单号 + 明细行幂等、单位换算、已生效状态过滤和门店编码映射；未验证前不得进入库存回算。
4. 飞书现有销售和 Brown Sugar BOM 继续只读使用，不回写原始表。

### P2：闭环与运营自动化

1. 为 D2 / D1 / S1 / T2 四类研判补齐统一的任务流转：创建日期、唯一任务号、来源流水、处理记录、结束原因与关闭按钮。
2. 配置机器人消息的正式规则：实时异常提醒和每日运营日报分别可编辑内容、接收范围、开关与发送时间；保留现有预览推送用于演示。
3. 盘点计划按“门店 + 营业日 + 任务来源”生成盘点单；每日盘点按策略纳入物料，工单或不定期盘点可自由选择物料。盘点提交后写入实盘流水，并自动触发理论与实盘差异研判。

### P3：经营指标数据层与 STORE001 模拟器

目标不是一次上线全部 KPI，而是先把数据来源、可信状态和计算口径做准，再分别服务“加盟商赚钱”和“总部管控”。

1. [ ] 定义经营指标数据契约；每个结果必须包含数值、单位、统计期间、来源引用、计算版本和 `confirmed / partial / estimated / unknown / not_integrated` 状态。
2. [ ] 锁定库存恒等式：期初 + 收货 + 调拨入 + 经确认调整 − BOM 消耗 − 报损 − 调拨出 = 理论期末；禁止直接修改当前库存而不生成来源单据。
3. [ ] 损耗拆为“已登记报损率、盘亏差异率、制作损耗率”，不使用一个笼统损耗率；制作损耗率等待制作批次、标准出成率和品牌合理损耗参数后启用。
4. [ ] 第一批只实现销售额/销量、已登记报损量、库存差异、可支撑天数、缺货/积压风险、订货/收货状态六类指标。
5. [ ] 毛利率先命名为“理论物料毛利率”，采购价为 Mock 时标记 `estimated`；不得表述为净利润。
6. [ ] 建立 `STORE001` 28 个营业日的版本化模拟数据集，使用真实 Brown Sugar BOM；模拟采购价、损耗率和支付数据必须标记 `data_origin=mock`、`scenario_id` 和 `dataset_version`，不得与真实飞书数据混算。
7. [ ] 模拟正常经营为主，并注入销量上涨缺货、销量下降积压、到货未确认、大额调拨未签收、报损升高、BOM 错误、kg/g 错误、重复收货八个可验证场景。
8. [ ] 加盟商首页只展示销售、利润/成本状态、缺货/积压和今日行动；总部运营中心按区域/门店/商品物料展示对比、执行和数据质量；研判卡仅把 KPI 作为上下文角标，不新增冗余规则页面。
9. [ ] 每个场景预先定义黄金结果，验证指标、V2 首要路径、建议动作、修正后重算和人工闭环。

### P4：加盟商订货支付与对账（第二阶段）

1. [ ] 先确认交易主体、供应商、总部定价、币种、税费、运费、预付/到付、余额/信用额度、退款和发票规则。
2. [ ] 订货单增加有效价格快照、应付金额、已付金额、支付状态、到期日、交易号、付款凭证和对账状态；价格后续变化不得改写历史订单金额。
3. [ ] 状态建议为：草稿 → 已提交 → 待支付 → 已支付 → 供应商确认 → 已发货 → 部分收货/已收货 → 已对账；取消与退款保留独立审计记录。
4. [ ] MVP 先使用模拟支付或上传凭证 + 人工确认，不接真实支付通道；支付动作不直接产生库存，确认收货才生成库存流水。

### 回测清单

- 报损：期初 + 入库 / 调拨入 − 报损 − 调拨出 − BOM 消耗 = 理论期末，且可从物料详情下钻到来源单据。
- 调拨：一对多调拨同时产生调出、调入两侧流水；调出门店不允许低于安全库存，外部仓调出不虚构门店库存。
- 门店提交：识别失败、字段缺失、确认提交、撤销和目标门店收货确认路径均可追溯；加盟商报损和调拨在 MVP 不增加总部前置审核。
- 飞书同步：重复拉取不会重复记账；未生效 / 字段不完整的单据只进入同步审计，不影响库存。

## 当前演示口径

- R2 是当前 Demo 的主状态存储，已可持久化库存动作、盘点、任务闭环、调拨演示及销售回算；D1 结构化表已完成 0012 / 0013 迁移并用于策略和研判数据，R2 继续保留为可用兜底。
- 定时同步会在跨营业日时将昨日各门店物料的理论期末固化为日结快照；今天有销售后，优先以该快照作为期初回算。历史快照只读保留，晚到数据需走调整 / 审计流程。
- 每日盘点计划按“门店 + 营业日”去重，只纳入标记为“每日盘点”的物料；“按需盘点”物料可由总部在不定期盘点或研判工单中自由选择下发。若当天尚未有销售，则以上一已固化日结快照生成当日计划单。
- 门店 HTML 调拨遵循双门店流转：调出门店提交即生成调出流水；调入门店确认收货后生成调入流水；总部只查看追溯与异常，不审核调拨。
- 库存汇总统一展示：期初、入库 / 调拨入、损耗 / 报损、调拨出、全部 SKU BOM 消耗、理论期末与状态。
- 报损目前可来自 R2 演示报损事件或飞书库存台账中的“报损量”；独立飞书报损表尚未绑定，故页面会明确标识来源或待接入状态。

## 已上线且已回测

- R2 Demo 状态存储：总部/门店的盘点闭环、主动运营任务、总部治理工单、凭证元数据、人工关闭记录、演示销售、首日物料期初及“重置演示”均持久化于 `inventory-count-closure-demo-state`。
- R2 保存当前状态、初始重置基线和历史快照；历史快照自动保留 60 天，`current.json` 与 `initial.json` 不自动删除。
- 门店与总部闭环已回测：盘点异常 → 总部要求复盘 → 门店提交凭证 → 总部关闭 → 重置到干净起点。
- 异常研判任务：操作列在首列，按治理策略提供下发门店核查、创建 BOM 工单、查看详情、误判关闭等交互；列表分页每页 10 条。
- 主动运营任务纳入闭环统计；总部可查看门店上传凭证；Demo 标识改为弱化角标 / 行状态，不再使用突兀的“演示”文字。
- `库存台账与追溯` 是台账主入口；飞书同步页保留为导入与同步审计入口。
- 台账页面已改为“物料优先”：顶层汇总不展示商品，点击物料的“查看来源”后才下钻到 SKU / 规格、销量、单件 BOM 用量、计算公式及该 SKU 的物料扣减。
- SKU011 高亮演示已可展示同物料被其他 SKU 消耗的拆分来源，避免把“其他 SKU 扣减”作为不可解释的黑盒数字。
- R2 台账演示已上线并验证：40 杯销售、13 项物料期初；半成品奶茶 `18 − (12×0.15 + 10×0.24) = 13.8kg`，冰块消耗 `12×0.19 + 10×0.12 = 3.48kg`。

## D1 / R2 当前状态

> 远程 D1 已备份并完成 `0012_demo_opening_inventory.sql`、`0013_material_inventory_diagnosis.sql`；R2 健康并继续作为当前状态和兜底存储。飞书定时批量同步是否恢复，仍以额度与真实链路验证结果为准。

1. `0012_demo_opening_inventory.sql` 已保存一轮演示销售所需的“首日全物料实盘期初”。
2. `0013_material_inventory_diagnosis.sql` 已保存按门店生效的物料安全库存策略，以及按“门店 + 日期 + 物料 + 规则”去重的研判结果。
3. `POST /api/demo-opening-inventory/initialize` 已支持：
   - STORE001 所有可用物料建立合理、非负的首日实盘期初；
   - 基线只存在 Demo 数据库，不回写飞书；
   - 次日开始按全部销售 SKU 的 BOM 汇总扣减。
4. 物料研判规则已启用；仍需用真实飞书数据继续回测：
   - 期初无效 / BOM、单位、映射、规则问题 → 总部直接治理；
   - 理论库存低于安全库存 → 补货 / 调拨，不默认让门店盘点；
   - 理论库存为负且基础数据可信 → 总部研判后决定是否下发门店；
   - 实盘与理论出现差异 → 门店复盘凭证 → 总部验收闭环。
5. 复核并关闭 / 改派此前“黑糖原料”门店核查任务：只有首日基线有效且差异依旧存在才保留门店动作，否则转为总部台账初始化治理。
6. 本地页面与规则已完成核心回测；真实飞书数据仍需验证物料期初非负、理论期末、贡献 SKU 下钻和研判任务去重。

## 后续：真实飞书销售数据流

1. 飞书多维表格作为原始销售、SKU 主档、BOM 和原始库存数据来源；不在飞书回写计算结果。
2. Demo / 正式数据库保存：导入批次、销售明细、SKU→BOM 展开、物料日消耗、门店物料台账、实盘记录、差异研判及工单关联。
3. 商品统计维度固定为 `SKU 编码`，规格不同即为不同 SKU；物料消耗按所有 SKU 的 BOM 汇总，不按商品名称混算。
4. 非 BOM 商品（如面包）保留销售记录并标注“未进入物料理论消耗”，不伪造扣减；同时生成可治理的 BOM 覆盖率依据。
5. D1 恢复后，将当前 R2 Demo 销售同步进入正式物料账本批次，确保不干扰原有盘点闭环 Demo，并保留“导入批次—计算版本”的可追溯关联。
6. 为每个物料复用牛奶台账样式：期初 + 收货/调拨 − 全部 SKU BOM 消耗 − 报损/调出 = 理论期末；支持查看流水来源和进入对应异常研判任务。
